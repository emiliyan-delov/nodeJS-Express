<!DOCTYPE html>
<html>
  <head>
    <title><%= site.title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>

    <%
        for(var i=0 ; i < items.length ; i++){
    %>
        <%- include('includes/content', { item: items[i]}); %>
    <%        
        }
    %>

  </body>
</html>
